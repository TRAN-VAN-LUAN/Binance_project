export enum inputTypes {
    TEXT = 'text',
    NUMBER = 'number',
    PASSWORD = 'password',
}

export enum buttonTypes {
    SUBMIT = 'submit',
}

export enum action {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    SUCCESS = 'success',
    INFO = 'info',
    WARNING = 'warning',
    HELP = 'help',
    DANGER = 'danger',
}
